import React, { Component } from 'react'
import { TextField } from '@material-ui/core'

class SearchField extends Component {
  render() {
    return <TextField label='Search field' type='search' />
  }
}

export default SearchField
